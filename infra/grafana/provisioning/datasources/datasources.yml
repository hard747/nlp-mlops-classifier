apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus-ds
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false

  - name: Postgres-Audit
    uid: postgres-audit-ds
    type: postgres
    access: proxy
    url: postgres_db:5432
    database: nlp_audit_db
    user: harre_admin
    secureJsonData:
      password: super_secure_password_2026
    jsonData:
      database: nlp_audit_db
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    editable: false
